window.onload = () => {
	document.querySelectorAll('button').forEach(
		b => b.addEventListener('click', handleClick)
	);
};

const handleClick = () => {
	// Determine what button was clicked and display rows accordingly
}
